---
import type { MenuEntry } from "@/types";
import EntryLayout from "@components/about/EntryLayout.astro";
import { getIndex } from "@lib/contentParser";

const entry = (await getIndex("menu")) as MenuEntry;
---

<EntryLayout entry={entry} />
